/**
 * @file src/app/(prototype)/alerts/page.tsx
 * @description 라우트 진입점 — 콘텐츠는 ./AlertsPage 컴포넌트에 위임
 */

import AlertsPage from "./AlertsPage";

export default function Page() {
    return <AlertsPage />;
}
