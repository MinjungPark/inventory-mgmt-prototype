/**
 * @file src/app/(prototype)/warehouse-inventory/page.tsx
 * @description 라우트 진입점 — 콘텐츠는 ./WarehouseInventoryPage 컴포넌트에 위임
 */

import WarehouseInventoryPage from "./WarehouseInventoryPage";

export default function Page() {
    return <WarehouseInventoryPage />;
}
